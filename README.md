# Soon ProductLed Assessment

Static HTML presentation for the Soon ProductLed assessment.

## Local preview

From this folder:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

## Deploy

This repository is intended to be published directly from the `main` branch root with GitHub Pages.
